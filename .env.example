# Discord bot environment configuration (example)

# 必填：Discord 機器人 Token（若缺少，容器應以退出碼 64 立即失敗）
DISCORD_TOKEN=

# （選填）限制允許的 Guild 清單；以逗號分隔的雪花 ID，留空則不限制
DISCORD_GUILD_ALLOWLIST=

# （選填）覆寫 Compose 預設資料庫連線字串；若未設定，Compose 會提供指向 postgres 服務的預設值
# 資料庫遷移將自動應用至最新版本 (head)
DATABASE_URL=postgresql://bot:bot@postgres:5432/economy

# （選用/dev）pgAdmin 服務設定（啟用需使用 profile dev）
# 若需在本機瀏覽，設定 PGADMIN_PORT（例如 8081）。
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=change-me
# 預設使用 8081；亦可改為其他未被占用的本機埠
PGADMIN_PORT=8081

# （選填）啟用轉帳事件池模式（預設：false，保持向後相容）
# 啟用後，轉帳請求將進入事件池進行異步檢查與自動重試
TRANSFER_EVENT_POOL_ENABLED=false

# （選填）每日轉帳上限（僅事件池檢查使用）；
# 未設定、空字串或 <=0 代表「無上限」（預設行為）
# 要啟用限制，設為正整數，例如：
# TRANSFER_DAILY_LIMIT=1000
